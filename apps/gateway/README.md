# Gateway Placeholder

This app is optional for local boot. In production it can front `site`, `docs`, and `blogs` with host-based rewrites.

Suggested future behavior:
- `lmnas.com` -> `apps/site`
- `docs.lmnas.com` -> `apps/docs`
- `blogs.lmnas.com` -> `apps/blogs`
