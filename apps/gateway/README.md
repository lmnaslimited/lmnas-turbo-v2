# Gateway placeholder

This app is a placeholder for future edge routing. Today, host-based routing should be handled by your reverse proxy:
- `lmnas.com` -> `apps/site`
- `docs.lmnas.com` -> `apps/docs`
- `blogs.lmnas.com` -> `apps/blogs`
