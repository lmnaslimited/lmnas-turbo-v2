{
  "name": "@lmnas/block-registry",
  "version": "0.0.0",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "lint": "echo lint block-registry",
    "test": "vitest run"
  },
  "dependencies": {
    "@lmnas/blocks": "workspace:*"
  }
}
