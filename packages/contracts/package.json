{
  "name": "@lmnas/contracts",
  "version": "0.0.0",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "lint": "echo lint contracts",
    "test": "vitest run"
  },
  "dependencies": {
    "@lmnas/blocks": "workspace:*",
    "zod": "^3.23.8"
  }
}
