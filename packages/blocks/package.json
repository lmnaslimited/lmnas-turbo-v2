{
  "name": "@lmnas/blocks",
  "version": "0.0.0",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "lint": "echo lint blocks",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "^3.23.8"
  }
}
