# Analytics — Codex Orientation

Handles event tracking.

## Rules
- Must not be directly used inside blocks.
- Triggered via conversionConfig.
- No hardcoded event names inside UI.
- Event names must align with productMapping.