# n8n Workflows

This folder stores versioned n8n workflow exports.

- Use `infra/docker-compose.yml` to run n8n locally on `http://localhost:5678`.
- Save exported workflow JSON files under `workflows/`.
