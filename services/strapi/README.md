# Strapi Service

Local CMS for LMNAs platform.

- Content type: `Page`
- Dynamic zone blocks: `hero`, `faq`
- Seeded page slug: `home`

Run with docker compose (recommended):
- `docker compose -f infra/docker-compose.yml up -d strapi postgres`
